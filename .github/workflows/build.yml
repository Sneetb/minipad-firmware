name: Build Minipad 2H2D Firmware

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: python -m pip install --upgrade platformio

      - name: Build 2-Hall + 2-digital firmware
        run: pio run -e minipad-2h2d-prod

      - name: Upload UF2
        uses: actions/upload-artifact@v4
        with:
          name: minipad-2h2d-uf2
          path: .pio/build/minipad-2h2d-prod/firmware.uf2
